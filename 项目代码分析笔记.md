# 心灵终结（Mental Omega）Mod 管理器 —— 代码分析笔记

> 本文是在开始新一轮编码前，对现有代码库做的一次通读式分析。目的是记录整体架构、核心机制、已知问题和风险点，方便后续修改时能快速定位、避免重复踩坑。

## 一、项目概况

- 这是一个 **WPF (.NET Framework 4.0) 桌面程序**，用于管理心灵终结（Mental Omega，红警2尤里的复仇的知名MOD）及其衍生游戏/任务包的"多版本切换 + 多存档配置(Profile)"。
- 核心创新点：**利用 NTFS 硬链接（Hard Link）** 把"基础游戏文件 + Mod 覆盖文件 + 存档配置文件"在运行前"拼装"到一个统一的运行目录（`Game/`），运行后再把变化的存档文件"收割"回 Profile 目录，用完即清空运行目录。本质上是一个基于文件系统的、无需真实复制文件的 overlay/联合挂载方案。
- 作者：Sad Pencil，README 中说明该方案不局限于心灵终结，可用于管理任意"文件覆盖式"程序。

## 二、技术栈与运行环境

- **UI 框架**：WPF，代码全部是传统"代码后置(code-behind)"风格，没有 MVVM、没有绑定框架（除了 XAML 里简单的 `{Binding}` 到 POCO 属性）。
- **目标框架**：`.NET Framework 4.0`（`Mo3ModManager.csproj` 中 `TargetFrameworkVersion=v4.0`），相当老，很多 C# 6+/7+ 语法糖和 BCL API 不可用。
- **依赖包**（`packages.config`）：
  - `Newtonsoft.Json 11.0.2`（用于解析 `node.json`）
  - `SharpZipLib 0.86.0`（用于解压 Mod 的 zip 包，`FastZip`）—— **这个版本非常老（约2011年前后），如果后续要处理不受信任的 zip 包，需要评估 zip slip（路径穿越）等已知历史漏洞，建议升级**。
- **运行权限**：`app.manifest` 中 `requestedExecutionLevel level="requireAdministrator"`，即整个程序默认要求以管理员身份运行。代码里没有显式解释原因，推测是因为程序把 `Mods/`、`Profiles/`、`Game/`、`Incoming/` 都建在自身安装目录（`AppDomain.CurrentDomain.BaseDirectory`）下，如果安装在 `Program Files` 等受保护目录，非管理员权限无法写入/创建硬链接目标文件；如果以后要去掉管理员权限要求，需要把可写目录移出受保护路径或改为按用户目录存储。
- **支持系统**：区分 Win7 及更早（`RunLegacyAsync`，退化为"轮询进程+提示用户手动确认"）与 Win8/10（`RunAsync`，用 Job Object + IO 完成端口精确等待所有子进程退出）。判断逻辑在 `MainWindow.xaml.cs` 的 `RunButton_Click` 里，用 `Environment.OSVersion.Version` 硬编码判断。

## 三、整体架构

代码没有分层（没有 ViewModel/Service/Repository），基本是"UI 事件直接调用业务逻辑 + 直接操作文件系统"的单层结构。大致模块划分：

```
MainWindow.xaml(.cs)      —— 唯一的主界面：Mod 树 + Profile 列表 + 操作按钮，承担了几乎所有业务逻辑的调用入口
InputWindow.xaml(.cs)     —— 通用的"输入一个字符串"弹窗（新建/重命名 Profile 时用）
Node.cs                    —— Mod 节点数据模型，对应磁盘上一个 Mod 文件夹里的 node.json
NodeTree.cs                —— 扫描 Mods/ 目录，构建 Mod 的树状继承关系（父子）
ModItem.cs                 —— Node 的 UI 包装（供 TreeView 绑定），持有 Node 引用
ProfileItem.cs              —— Profile 的 UI 包装（供 ListView 绑定），计算目录大小
ModProcessManager.cs        —— 核心运行时逻辑：准备硬链接 -> 启动游戏并等待退出 -> 清理/回写存档
ModProcessManagerArguments.cs —— 传给 ModProcessManager 的参数 DTO
IO.cs                       —— 硬链接/目录复制/"判断两文件是否同一物理文件"等文件系统工具方法
Win32.cs                    —— 大量 P/Invoke 声明（CreateHardLinkW、Job Object、CreateProcessW、IO 完成端口等）
```

没有单元测试项目，没有 CI 配置（仓库里只有 `.sln` + 单个 WPF 项目）。

## 四、核心概念与数据模型

### 1. Node / NodeTree（Mod 节点树）—— 系统的核心抽象

- 每个 Mod 是磁盘上 `Mods/<某文件夹>/` 下的一个目录，必须包含：
  - `node.json`：描述该节点的元数据（`id`、`name`、`main_executable`、`arguments`、`parent`、`compatibility`）
  - `Files/`：该节点相对于父节点的**增量文件**（硬链接覆盖用）
- `parent` 字段（对应 `Node.ParentID`）建立起 Mod 之间的**继承链**：一个 Mod 可以"基于"另一个 Mod，运行时会从根节点到叶子节点依次把各级 `Files/` 硬链接叠加到运行目录，从而实现"底包 + 增量任务包"的组合，无需为每个组合都完整复制一份游戏文件。
- `IsRoot`：`ParentID` 为空即为根节点（通常是"完整游戏本体"），根节点在 UI 树中作为顶层节点显示。
- `IsRunnable`：`MainExecutable` 非空则可运行（叶子任务包可能没有可执行文件，只是父级的数据补丁，不能直接运行，只能作为"中间层"存在）。
- `NodeTree` 用 `Dictionary<string,Node>` 以 `ID` 为键建立索引，再用 `ParentID` 关联出 `Parent`/`Childs` 关系，最终收集所有根节点到 `RootNodes`。

### 2. Profile（存档配置）

- 对应 `Profiles/<Profile名>/` 目录，存放某个 Mod 运行后产生的存档/配置文件（`.INI` 等）。
- `ProfileItem` 只是 UI 层的展示模型（名称 + 目录 + 计算出的大小 MB），本身不含业务逻辑。
- 注意：一个 Profile 目录实际上是按 `Profiles/<Profile名>/<Node.ID>/` 存的（见 `MainWindow.RunButton_Click` 里 `ProfileDirectory` 的拼接），即**同一个 Profile 下，不同 Mod 节点的存档是分开存储、互不覆盖**的。

### 3. 运行期临时目录

- `Game/`：实际拉起游戏进程的工作目录，运行前"拼装"，运行后清空。
- `Incoming/`：安装 Mod 时 zip 的解压临时目录，装完就清空。

## 五、关键业务流程

### 1. 启动流程 (`MainWindow` 构造函数)
`BuildTreeView()` 扫描 `Mods/` 构建 `NodeTree` 并填充 TreeView；`BuildProfiles()` 扫描 `Profiles/` 填充 Profile 列表。任何异常都会弹窗并 `Environment.Exit(1)` 直接退出程序——**启动阶段没有容错，一个损坏的 node.json 都可能让程序无法启动**（需与下面的"问题清单"对照看）。

### 2. 安装 Mod (`InstallModButton_Click`)
1. 用 `OpenFileDialog` 选一个 `.zip`。
2. 用 `SharpZipLib.FastZip` 解压到 `Incoming/`。
3. 用一个"影子" `NodeTree`（浅拷贝当前树）尝试把 `Incoming/` 加进去做校验：若解压出的内容没有产生新节点（`Count()` 相等）则认为是无效包，抛异常。
4. 校验通过后，用 `IO.CreateHardLinksOfFiles` 把 `Incoming/` 的目录结构和文件**硬链接**（不是复制）搬到 `Mods/` 下。
5. `finally` 里清空 `Incoming/`。
6. 重新 `BuildTreeView()` 刷新界面。

代码注释里作者自己也提到了"影子树"只是浅拷贝，`testTree` 和 `this.NodeTree` 会共享同一批 `Node` 对象引用，属于已知的技术债（见问题清单）。

### 3. 运行游戏 (`ModProcessManager`) —— 全部逻辑的核心

三步走：

**Step1 `RunStep1_Prepare`**：
- 确保 `ProfileDirectory` 存在；清空并重建 `RunningDirectory`（`Game/`）。
- 先把 Profile 目录内容硬链接进 `Game/`（存档优先叠加）。
- 再调用 `PrepareNode(Node)`：从当前选中节点开始，**递归到根节点**，依次把每一级的 `Files/` 目录硬链接进 `Game/`（`.INI` 后缀走复制而非硬链接，避免游戏运行时写 INI 影响到 Mod 源文件）。
- 通过写 `HKCU\...\AppCompatFlags\Layers` 注册表项设置 `MainExecutable` 的兼容性模式（`Node.Compatibility` 字段）。
- 有一条 `TODO: Set the registry to avoid firewall` 未完成。

**Step2 `RunStep2_RunAndWait`**（仅 Win8+）：
- 手写 P/Invoke：`CreateJobObjectW` + `CreateIoCompletionPort` + `SetInformationJobObject`(关联完成端口) + `CreateProcessW`(`CREATE_SUSPENDED`挂起创建) + `AssignProcessToJobObject` + `ResumeThread`，再用 `GetQueuedCompletionStatus` 阻塞等待 `JOB_OBJECT_MSG_ACTIVE_PROCESS_ZERO` 消息。
- 这是标准的"等待一个进程及其所有子/孙进程全部退出"的经典 Windows 技巧（参考老外 Raymond Chen 的博客，代码注释里也贴了链接），比单纯 `Process.WaitForExit()` 更可靠，因为很多游戏会启动子进程（launcher 拉子进程）主进程本身可能先退出。
- Win7 及更老系统走 `RunLegacyAsync`：没有这套机制，退化为"启动后 sleep 10s，然后弹窗让用户自己确认游戏已退出"，还写了一段"通过遍历所有进程、判断其路径是否在运行目录下"的辅助确认逻辑，体验比较原始。

**Step3 `RunStep3_Clean`**：
- `CleanNode(Node)`：递归从叶子到根，对每一级 `Files/` 里的每个文件，判断 `Game/` 里对应文件是否与源文件"是同一个物理文件"（`IO.IsSameFile`，用 `GetFileInformationByHandle` 比较卷序列号+文件索引号），如果是（说明没被游戏改写过），就删掉这个硬链接；如果不是（说明游戏在运行时改写了这个文件，产生了新的物理文件），则保留在 `Game/` 里不删——这些"幸存"下来的文件就是需要收割进 Profile 的存档变更。
- `IO.RemoveEmptyFolders`：递归删除空文件夹。
- 如果 `Game/` 没被完全清空（说明有存档变化），就把 `Game/` 剩下的内容硬链接（这次是 `Override=true`）写回 `ProfileDirectory`。
- 最后 `IO.ClearDirectory(Game/)` 保证运行目录恢复到干净状态，供下次使用。

这套"硬链接叠加 + 差异检测回写"的机制，本质上是自己实现了一个轻量级的 Union FS / Overlay FS，且是通过对比"文件是否为同一 inode"来判断"是否被改写"，设计思路挺巧妙，但依赖 NTFS 特性（不支持跨磁盘分区、不保证在 ReFS 等文件系统上可靠，代码注释里也承认了这一点）。

## 六、目录结构与运行时文件布局约定（隐式契约，未在文档中写明）

```
<程序安装目录>/
├── Mods/                     所有已安装 Mod（每个子目录含 node.json + Files/）
├── Profiles/<Profile名>/<NodeID>/   每个 Profile 下按 Mod 节点分别存档
├── Game/                     运行时临时拼装目录（应始终保持"用完即空"）
└── Incoming/                 安装 Mod 时的解压临时目录
```

这套目录结构完全是隐式约定（散落在各处硬编码的字符串常量 `"Mods"`、`"Profiles"`、`"Game"`、`"Incoming"`、`"Files"`、`"node.json"`），没有集中的配置/常量类。以后如果要重构，建议先把这些路径常量提取到一处。

## 六.五、修复记录

> 记录已经实际落地的修改，避免和"问题清单"混淆。每条修改都注明对应第七节的问题编号、改动范围和验证方式。

### 批次0（编译环境搭建时的必要改动）

- **`Mo3ModManager.csproj` 的 `TargetFrameworkVersion` 从 `v4.0` 改为 `v4.8`**
  - 原因：Visual Studio 2022 Build Tools 已不再分发 `.NET Framework 4.0` 的 Targeting Pack（该版本发布于2010年，微软已从新版工具链下架），无法在当前机器上编译原始的 `v4.0` 目标。
  - 影响评估：4.0 到 4.8 之间是严格向后兼容的，现有代码无需任何改动即可在 4.8 上编译运行。选择 4.8（而非折中版本）是因为它是 4.x 系列的最终版本，兼容性和长期支持最好。
  - 验证：编译通过，`bin\Debug\Mo3ModManager.exe` 正常生成。未做运行时功能验证。

### 批次1（低风险问题修复，编译验证通过）

1. **修复 `NodeTree.cs` 的 `Files` 目录校验 Bug**（对应问题清单第1条）
   - 位置：`NodeTree.GetNodesFromDirectory`
   - 修改前：`Debug.Assert`/判空逻辑误用了 `nodeFiles`，导致 `filesFolders` 从未被真正校验，没有 `Files/` 子目录的 Mod 文件夹也会被当作合法节点加载。
   - 修改后：改为正确校验 `filesFolders`，缺少 `Files/` 目录的 Mod 会在加载阶段被跳过，而不是留到运行阶段才抛 `DirectoryNotFoundException`。

2. **`NodeTree.cs` 解析失败的静默吞异常改为记录日志**（对应问题清单第2条的一部分）
   - 位置：`NodeTree.GetNodesFromDirectory` 里 `Node.Parse` 的 `catch` 块
   - 修改内容：`catch (Exception) { continue; }` 改为在 `continue` 前用 `Trace.WriteLine` 记录出问题的 Mod 目录和异常信息（`[Warn] Failed to parse mod at "..."：...`）。
   - 不改变原有的"跳过该 Mod、程序继续启动"的行为，只是让排查问题时能看到原因。

3. **`NodeTree.cs` 的 `BuildTree` 报错信息优化**（对应问题清单第3条提到的"ID 冲突报错不友好"）
   - 位置：`NodeTree.BuildTree`
   - 修改内容：把原来只报 ID 的 `"Node xxx already exists."` / `"Node xxx not exists."`，改成包含 Mod 名称、目录路径、冲突/缺失的 ID、以及关联的另一个 Mod 信息的完整描述，方便用户定位是哪个 Mod 文件的 `node.json` 出了问题。
   - 只改动异常消息文本，不改变异常抛出的时机和判断条件，属于纯文案改进。

4. **`Win32.cs` 修正 Job Object 相关结构体的字段类型**（对应问题清单第5条）
   - 位置：`JOBOBJECT_BASIC_LIMIT_INFORMATION`（`MinimumWorkingSetSize`/`MaximumWorkingSetSize`/`Affinity`）、`JOBOBJECT_EXTENDED_LIMIT_INFORMATION`（`ProcessMemoryLimit`/`JobMemoryLimit`/`PeakProcessMemoryUsed`/`PeakJobMemoryUsed`）
   - 修改内容：对应原生 `SIZE_T`/`ULONG_PTR`（指针宽度，64 位下为 8 字节）的字段，从 `uint`（固定 4 字节）改为 `System.UIntPtr`（随进程位数变化，正确匹配原生结构体内存布局）。
   - 这两个结构体目前未被任何代码实际使用（只有 `JOBOBJECT_ASSOCIATE_COMPLETION_PORT` 被 `ModProcessManager` 使用，其字段本身就是指针类型，未受影响），改动不影响现有编译产物和运行时行为，属于预防性修正。

**验证方式**：以上4项改动完成后，用 `MSBuild.exe` 重新编译整个解决方案（`Configuration=Debug, Platform=Any CPU`），编译 0 错误，`bin\Debug\Mo3ModManager.exe` 正常生成。由于这几处改动都不涉及运行时会触发的主流程差异（Bug修复只影响"损坏的 Mod 是否被加载"这种边界情况，结构体改动的两个类型完全未被引用），仅做了编译验证，未做端到端运行测试。

### 批次2（问题8分析、问题9取消管理员权限、问题11本地化+语言切换，均已手动运行验证）

1. **问题8：游戏退出检测机制 —— 分析结论，未改代码**
   - 结论：Win8+ 路径（`ModProcessManager.RunStep2_RunAndWait`，Job Object + IO 完成端口监听 `JOB_OBJECT_MSG_ACTIVE_PROCESS_ZERO`）已经是 Windows 官方推荐的、等待"进程及其所有子孙进程退出"的最佳实践（参考微软文档与 Raymond Chen 的博客原文，代码注释里引用的正是这篇），不需要改。
   - 体验差的是 Win7 及更早的降级分支 `RunLegacyAsync`（`Sleep(10秒)` + 弹窗人工确认），原因是 Win8 之前不支持嵌套 Job Object，若被启动的游戏进程本身已属于某个 Job，`AssignProcessToJobObject` 会在老系统上失败，所以这层版本判断是有意为之的防御性代码。
   - 建议：Windows 7 官方支持已于2020年结束，ESU 也已到期，如果不再需要兼容 Win7，可以考虑整体移除 `RunLegacyAsync` 分支。这是产品范围决策，未经用户确认不会执行，本次未做代码改动。

2. **问题9：取消管理员权限要求**（对应问题清单第9条）
   - 位置：`app.manifest`
   - 修改内容：`requestedExecutionLevel` 从 `level="requireAdministrator"` 改为 `level="asInvoker"`。
   - 依据：代码里没有任何地方显式需要管理员权限——硬链接创建（`CreateHardLinkW`）本身只需要对目标卷有写权限；`SetCompatibility` 写的是 `HKCU`（当前用户级注册表），不需要管理员；`Mods/`、`Profiles/`、`Game/`、`Incoming/` 都建在程序自身目录下，只有当安装目录本身是受 UAC 保护的路径（如 `Program Files`）时才需要管理员权限。
   - 验证：编译通过，实际运行未再弹出 UAC 确认框，程序正常启动。

3. **问题11：本地化功能（中英文界面 + 手动语言切换）**（对应问题清单第11条）
   - **实现方式**：所有用户可见的界面文案（窗口标题、按钮、GroupBox 标题、确认/警告对话框文字等）已抽取到强类型资源类 `Properties.Resources`。异常消息(`ex.Message`)和 `Trace.WriteLine` 诊断日志保持英文不做本地化（面向开发者/排查问题，混杂路径和错误码，符合业界惯例）。
   - **资源存储方式**：按用户要求保留为**内嵌资源**（编译进主程序集），未做成外部 XML/JSON 配置文件。英文资源在 `Properties\Resources.resx`，中文资源在 `Properties\ResourcesChs.resx`。
     - 关键实现细节：中文资源**没有**使用 .NET 内置的"卫星程序集"(satellite assembly)机制（即没有用 `Resources.zh-Hans.resx` 这种会被 MSBuild 自动识别的命名），因为该机制依赖 Windows SDK 的 `al.exe`（Assembly Linker）在编译期生成卫星程序集，而当前机器只装了 Visual Studio Build Tools（不含完整 Windows SDK），装 SDK 对"仅两个语言文件"这个目标来说过重。
     - 改为：`ResourcesChs.resx` 作为普通嵌入资源直接打进主 exe，`Resources.Designer.cs` 手写了 `GetLocalizedString(name)` 方法，运行时根据 `Culture.TwoLetterISOLanguageName == "zh"` 手动选择从英文还是中文的 `ResourceManager` 读取字符串，对外暴露的 `Properties.Resources.XXX` 属性接口不变。
   - **手动语言切换**：主窗口新增"语言..."按钮，点击弹出菜单（跟随系统语言 / English / 简体中文），选择后立即刷新界面文案并持久化到用户设置。
     - 新增 `LocalizationManager.cs`：封装"应用语言 / 加载已保存设置 / 保存设置 / 语言显示名称"。
     - 新增 `Properties.Settings.Language`（字符串，用户级作用域），持久化到 `user.config`；补充了 `App.config`（`ApplicationSettingsBase` 持久化用户设置依赖它，项目原来没有这个文件）以及 `Settings.settings`/`Settings.Designer.cs` 里对应的设置项（这几个 Designer 文件本应由 VS 工具生成，因为是命令行 MSBuild 编译，手动同步的，逻辑等价）。
     - `App.xaml.cs` 的 `OnStartup` 里在任何窗口构造之前加载并应用已保存的语言。
   - **踩坑记录（供后续参考）**：
     - 最初尝试用 XAML `{x:Static properties:Resources.XXX}` 绑定按钮文案，编译能通过，但运行时崩溃（`XamlParseException` → `ArgumentException`，"无法将 xxx 作为 StaticExtension 值，因为它不是枚举、静态字段或静态属性"）。根因：VS 为 `.resx` 生成的强类型资源属性默认是 `internal`，`x:Static` 通过反射查找时只认 `public` 成员。解决方式：改为在 code-behind 的 `ApplyLocalizedText()` 里直接给已命名的 XAML 元素（如 `AboutButtonText.Text`）赋值，不依赖 `x:Static`。
     - "关于"按钮悬停文案切换语言后不生效，直到再次悬停才更新：根因是 `AboutButton_MouseEnter`/`MouseLeave` 原来的写法是 `Button.Content = new AccessText() {...}`，每次悬停都创建一个新对象换掉 `Content`，导致语言切换时更新的是 XAML 里那个已经"失联"的旧对象。改为直接修改已有对象的 `.Text` 属性（`this.AboutButtonText.Text = ...`）后解决。
     - 编程方式打开的 `ContextMenu`（`IsOpen = true`，非用户右键触发）默认没有锚点，会显示在屏幕左上角(0,0)。解决方式：打开前显式设置 `LanguageContextMenu.PlacementTarget = this.LanguageButton`。
   - **验证方式**：编译通过（0 错误），手动运行验证：界面正常显示中/英文文案、点击语言按钮菜单正确定位在按钮下方、切换语言后所有文案（包括"关于"悬停文案）立即刷新、关闭重启程序后记住上次选择的语言、未再触发 UAC 提示。
   - **一个反复出现的坑，值得单独记一下**：WPF 里定义在 `Window.Resources` 里的元素（比如共享的 `ContextMenu`），其内部用 `x:Name` 标记的子元素**不会**注册到窗口的命名作用域(namescope)，不能用 `this.元素名` 这种生成字段的方式直接引用（会编译报错 `CS1061`）。必须改用 `((ContextMenu)this.Resources["key"]).Items[索引]` 这种方式按资源键+索引取。这个坑在后续做右键菜单相关功能（重命名、隐藏、改ID）时又踩了几次，每次加菜单项都要注意同步检查索引是否偏移。

### 批次3（修复"影子树"共享引用污染问题，对应问题清单第3条第一点）

- **问题本质**：`InstallModButton_Click` 里原来用 `NodeTree testTree = new NodeTree(this.NodeTree)` 造一棵"影子树"来校验压缩包内容，但拷贝构造函数只新建了 `Dictionary`/`List` 容器本身，容器里的 `Node` 对象和 `this.NodeTree` 中的是同一批引用。当校验批次里既有合法节点（会被 `BuildTree` 真实执行 `Parent.Childs.Add(node)`，即修改了真实树里已存在节点的 `Childs` 列表）又有不合法节点（导致中途抛异常）时，安装失败后 `this.BuildTreeView()` 不会被调用来重建真实树，真实树中已存在节点的 `Childs` 列表就会留下一个"幽灵"子节点（磁盘上不存在，内存模型却认为存在），直到下次 `BuildTreeView()` 被触发前一直保持污染状态。
- **修复方案**：不再构造"影子树"，改为在 `NodeTree` 上新增只读校验方法：
  - `ValidateNodesCanBeMerged(List<Node>)`（私有）：只检查 ID 冲突（新批次内部之间、新批次与现有树之间）和父节点引用能否解析（现有树或新批次内部），全程不对任何 `Node` 对象赋值 `Parent`/`Childs`，也不修改 `this.NodesDictionary`/`RootNodes`，因此不可能污染任何共享对象。
  - `ValidateNodes(string Directory)`（公开）：扫描目录解析出节点列表，调用上面的校验，返回节点数量（供调用方判断"压缩包是否为空"），供 `InstallModButton_Click` 使用，替代原来的"影子树 + Count() 比较"写法。
  - `BuildTree`（真正建树用的方法，`AddNodes` 内部调用）改为先调用 `ValidateNodesCanBeMerged` 做完整性检查，检查通过后才批量执行赋值，从而保证"要么全部成功建树，要么完全不修改"的原子性。
  - 同时移除了不再被使用的 `NodeTree(NodeTree)` 拷贝构造函数（全代码库唯一调用点就是这次被替换掉的"影子树"逻辑）。
- **验证方式**：
  - 编译通过（0 错误），手动运行确认程序基本功能未受影响。
  - 额外编写了一个临时的最小化验证程序（引用 `Node.cs`/`NodeTree.cs` 源码，未纳入正式项目，验证后已删除）复现原问题场景：现有树中有根节点 X；一个"新增批次"里同时包含 ModA（合法，是 X 的子节点）和 ModB（不合法，父节点 ID 不存在）。验证结果：`ValidateNodes` 正确抛出异常，且抛异常前后 `X.Childs.Count` 始终为 0（未被污染），确认修复生效。

### 批次4（回退批次2第2条：恢复管理员权限要求）

- **背景**：批次2把 `app.manifest` 的 `requestedExecutionLevel` 从 `requireAdministrator` 改成了 `asInvoker`，理由是"代码里没找到显式需要管理员权限的操作依据"。这个判断只基于静态代码审查，没有考虑到 Mod 管理器实际拉起的目标是**用户配置的游戏可执行文件**，而不是只有 Mod 管理器自身的逻辑。
- **实际问题**：改为 `asInvoker` 后，用户实测点击"运行"直接报错弹窗：`WinAPI CreateProcessW failed. Error 740`。`740` 是 Windows 错误码 `ERROR_ELEVATION_REQUIRED`，含义是"目标可执行文件的清单要求管理员权限，但调用进程（Mod 管理器自身）当前是普通权限，Windows 拒绝静默创建该子进程"。也就是说，是游戏本体（`Node.MainExecutable` 指向的 exe）自身的清单要求了管理员权限，Mod 管理器降权后就没有能力拉起它了。
- **修复**：把 `app.manifest` 的 `requestedExecutionLevel` 改回 `requireAdministrator`。已重新编译并确认打包进 exe 资源里实际生效的清单条目确实是 `requireAdministrator`（不是被注释掉的示例文本）。
- **结论修正**：管理员权限要求**不是** Mod 管理器自身不需要的冗余配置，至少对于要求提升权限的游戏本体，这是必需的——父进程的权限级别不能低于要被拉起的子进程要求的权限级别。这一条问题应视为"设计如此，非缺陷"，不再作为待优化项。
- **经验教训**：这类"降低权限要求"的改动，仅靠静态代码审查（搜索代码里有没有显式调用需要管理员权限的 API）是不够的，必须做端到端的实际运行验证（真正点击"运行"走一遍拉起子进程的流程）才能发现这类因"目标程序自身清单要求"引发的运行时问题。

### 批次5（用户体验改进：刷新/单实例/记住选择、手动安装向导、Mod 改名/隐藏/改ID/打开目录）

这一批是根据用户实际使用反馈新增的功能性改动，不是修 bug，是产品体验迭代。按子批次记录：

**批次5-A（低风险，均已手动验证通过）**：
- **刷新按钮**：`RefreshButton_Click` 调用 `BuildTreeView()`/`BuildProfiles()` 重新扫描磁盘，并在重建前后记住/恢复当前选中的 Mod 和 Profile（否则每次刷新都会丢失选中状态）。
- **单实例限制**：`App.xaml.cs` 的 `OnStartup` 用命名 `Mutex`（固定 GUID 命名，避免和其他程序冲突）检测是否已有实例在跑，如果有则弹警告并退出。**踩坑**：`Mutex` 的 `initiallyOwned: true` 只在**真正创建**该 Mutex 时才代表当前线程持有锁；如果 Mutex 已存在（第二个实例的情况），当前线程并不持有它，对它调用 `ReleaseMutex()` 会抛 `ApplicationException`。修复方式是只有 `isNewInstance == true` 时才保留 Mutex 引用用于后续释放，`OnExit` 里释放时也加了 try/catch 兜底（避免退出流程本身变成新的崩溃源，掩盖真正的异常）。
  - **另一个更隐蔽的坑**：实现这一批功能过程中用户反复遇到启动即报错"创建 userSettings/... 的配置节处理程序时出错：未能从程序集 System.Configuration 中加载类型 System.Configuration.ClientSettingsSection"。根因是 `App.config` 里手写这两个配置节类型的完全限定名时，把所属程序集写成了 `System.Configuration`（连带 PublicKeyToken 也抄错了），但 `ClientSettingsSection`/`UserSettingsGroup` 这两个类型实际定义在 `System.dll`（PublicKeyToken 应为 `b77a5c561934e089`）里，`System.Configuration.dll` 是完全不同的另一个程序集。改成 `System.Configuration.ClientSettingsSection, System, Version=4.0.0.0, ...` 后解决。排查过程中一度误判为"缺少 `System.Configuration` 程序集引用"而在 `.csproj` 里加了引用，这个引用本身没有错（后续用到 `ApplicationSettingsBase` 确实需要），但不是这次报错的真正原因，属于中途走了一次弯路，最终定位到是 `App.config` 里的程序集名写错。
- **记住并自动恢复上次选择的 Profile/Mod**：新增用户设置 `LastProfileName`/`LastModID`（持久化到 `user.config`），启动时自动应用；没有保存记录时默认选中第一个 Profile。同时修复了"运行游戏后 Profile 列表自动清空选择"的问题（根因是 `RunWorkerCompleted` 回调里重建整个列表，之前没有保留选中状态的逻辑）。

**批次5-B（中等复杂度，均已手动验证通过）**：
- **手动安装向导**（`InstallModWizard.xaml(.cs)` + `FolderTreeNode.cs`）：解决"绝大多数真实 Mod 压缩包都没有为这个工具预置 `node.json`，导致安装必须手动解压+手写配置"的核心痛点。逻辑：解压后先用 `NodeTree.ValidateNodes` 检测是否已经符合本工具约定（有 `node.json`），如果没有，则弹出向导——左侧是解压出来的文件夹树（可展开选择任意层级作为 `Files/` 内容），右侧是表单（名称/ID自动生成可编辑/主程序下拉框自动扫描.exe/启动参数/父Mod下拉框/兼容模式）。兼容模式字段默认预填 `"~ RUNASADMIN HIGHDPIAWARE"`（根据用户反馈，mo3.club 官方整合包对心灵终结引擎程序都用这个组合：`RUNASADMIN` 请求管理员权限，`HIGHDPIAWARE` 避免老游戏在高分屏下被系统强制拉伸模糊）。`Node.cs` 新增 `Write(directory)` 方法支持序列化回 `node.json`（原来只有 `Parse` 读取）。
- **右键改名**：右键菜单加"重命名..."项，只更新 `node.json` 的 `name` 字段，不改文件夹名（避免不必要的硬链接路径变动风险）。
- **右键隐藏 + 显示隐藏项开关**：新增 `ModDisplayState.cs`，把"隐藏"这个纯 UI 偏好持久化到 `Mods/ModManagerState.json`（刻意不写进各 Mod 自己的 `node.json`，因为这是本地展示偏好不是 Mod 本身属性；这个文件不影响 `NodeTree` 扫描，因为扫描只看子目录不看文件）。`ModItem.cs` 新增支持过滤的构造函数重载，隐藏一个节点会让其整个子树自然级联消失（不需要给每个子节点单独打标记）。`DeleteModButton` 的可用性判断特意改为检查真实的 `Node.Childs`（而非可能被过滤的 UI 树 `Items`），避免在"子节点被隐藏"时误判成"没有子节点"从而允许误删。删除 Mod 时会顺手清理该 ID 在隐藏状态文件里的残留记录。
  - 追加需求：隐藏的 Mod 在"显示隐藏项"勾选后要有视觉区分，做法是 `ModItem` 新增 `IsHidden` 属性，`Title` 自动追加本地化的"(已隐藏)"后缀，XAML 里用 `DataTrigger` 把文字颜色设为灰色。

**批次5-C（高风险，已实施且通过自动化+人工验证）**：
- **右键更改 Mod ID**（含级联迁移）：新增独立的 `ModIdChanger.cs`（不直接写进 UI 代码，方便脱离真实交互做自动化验证）。Mod 的 ID 不只是标签，还被用作：(1) 直接子 Mod `node.json` 里 `parent` 字段引用的值；(2) 每个 Profile 下存档文件夹的名字（`Profiles/<Profile>/<ModID>/`）。只改自己的 `node.json` 而不做级联迁移，会导致子 Mod 变成"父节点不存在"、且**所有 Profile 下该 Mod 的存档会被静默丢弃**（游戏会在新 ID 路径下开始一个空存档，旧存档还在磁盘上但再也不会被读取）。
  - 迁移顺序：先重命名所有 Profile 下的存档文件夹 → 再更新所有直接子 Mod 的 `parent` 字段 → 最后更新自身 `node.json` 的 `id`。任何一步失败都会尽力回滚已完成的步骤（例如子节点更新失败会把已改的子节点 `parent` 改回去，Profile 文件夹改名失败也会试着改回原名），但这是"尽力而为"的回滚，不是数据库事务级别的原子性保证。
  - `NodeTree.cs` 新增 `ContainsID`（改ID前检查是否与现有 Mod 冲突）和 `ReKeyNode`（`NodesDictionary` 是以 ID 为键的字典，只改 `Node.ID` 属性不会自动更新字典键，必须显式重新插入，否则用新 ID 查找会失败）。
  - `InputWindow.ShowDialog` 加了可选的预填文本参数（改ID时默认显示当前ID方便编辑，不影响其他调用点）。
  - **验证方式**：除了编译+人工点击验证，额外写了一个独立的自动化测试程序（复用真实源码 `Node.cs`/`NodeTree.cs`/`ModIdChanger.cs`，未纳入正式项目，验证后已删除），模拟"根节点X + 子节点Y(parent=X) + 两个Profile各有X的存档 + 其中一个Profile还有不相关的Y存档"的场景，验证改ID后 X自身、Y的parent字段、两个Profile下X的存档文件夹全部正确更新且文件内容完整保留，Y的不相关存档完全没被碰过；另外验证了目标ID已存在存档文件夹时会拒绝执行且不留下部分修改的残留状态。16项检查全部通过。
- **右键打开所在目录**：菜单最上方加"打开所在目录..."项，用 `Process.Start(Node.Directory)` 打开该 Mod 在 `Mods/` 下的文件夹，跟 `ProfilesListView` 原有的双击打开逻辑保持一致的实现方式。

**批次5 全部改动的通用验证方式**：每个子功能实现后都用 `MSBuild.exe` 重新编译确认 0 错误，并实际启动 `Mo3ModManager.exe` 确认无崩溃，关键功能点由用户手动交互验证（安装向导走一遍完整流程、改名/隐藏/改ID/打开目录分别操作确认）。

## 七、代码问题与风险点清单（按严重程度粗排）

1. ✅ **已修复（见"六.五、修复记录"第1条）** ~~`NodeTree.GetNodesFromDirectory` 里有复制粘贴 Bug~~（`NodeTree.cs`）：
   ```csharp
   // Files folder must exist
   var filesFolders = modFolder.GetDirectories("Files", System.IO.SearchOption.TopDirectoryOnly);
   System.Diagnostics.Debug.Assert(nodeFiles.Length <= 1);   // 应该断言 filesFolders
   if (nodeFiles.Length == 0) continue;                      // 应该判断 filesFolders.Length==0
   ```
   本意是校验 `Files/` 子目录必须存在，但实际判断条件写的是已经判断过的 `nodeFiles`，`filesFolders` 变量被计算出来却从未真正用于任何判断。结果是：**一个只有 `node.json`、没有 `Files/` 目录的 Mod 文件夹也会被当作合法节点加载**，等到运行阶段 `PrepareNode`/`CleanNode` 访问 `Node.FilesDirectory`（不存在的目录）时会抛 `DirectoryNotFoundException`，报错时机被推迟、报错信息也不直观。这是一个应该修的真 Bug。

2. **异常处理粒度太粗，且大量"静默吞掉"**：
   - ✅ **已修复（见"六.五、修复记录"第2条）** ~~`NodeTree.GetNodesFromDirectory` 里 `catch (Exception) { continue; }`（`NodeTree.cs`），任何解析失败的 Mod 会被无声跳过，不会有任何日志或提示告诉用户"哪个 Mod 损坏了、为什么"。~~ 现已在跳过前记录 `Trace` 日志。
   - 几乎所有 UI 事件处理器都是 `catch (Exception ex) { MessageBox.Show(ex.Message, ...) }`，直接把底层异常消息（可能是英文的 WinAPI 错误码文本）扔给用户，缺乏用户可读的错误分类。**（未修复，涉及面广，留待后续批次）**
   - 应用启动阶段 (`MainWindow` 构造函数) 任何异常都直接 `Environment.Exit(1)`，没有重试或降级路径。**（未修复）**

3. ✅ **已修复（见"六.五、修复记录"批次3）** ~~`InstallModButton_Click` 里"影子树"校验逻辑薄弱~~（`MainWindow.xaml.cs`）：
   - ~~`NodeTree testTree = new NodeTree(this.NodeTree)` 只是浅拷贝 `Dictionary`/`List`，`Node` 对象本身是共享引用，校验批次中途失败会污染真实树里已存在节点的 `Childs` 列表~~。已改为只读校验方法 `NodeTree.ValidateNodes`，不再构造共享引用的"影子树"，也移除了不再使用的 `NodeTree(NodeTree)` 拷贝构造函数。
   - ✅ **报错信息已优化（见"六.五、修复记录"第3条）**：`BuildTree` 中 ID 冲突/父节点缺失的异常消息，现已包含 Mod 名称、目录路径、具体冲突原因，不再是原来的 `"Node xxx already exists."` 这种不直观的文案。

4. **依赖库版本非常老，存在潜在安全风险**：
   - ✅ **已解决（见"六.五、修复记录"批次6）** ~~`SharpZipLib 0.86.0`（约 2011 年发布）：用来解压**用户手动选择的 zip 包**（`InstallModButton_Click`），老版本压缩库历史上出现过路径穿越（zip slip）等问题~~。已替换为 `SharpCompress 0.23.0`（支持 zip/rar/7z/tar 等多格式），且新增了解压后目标路径的穿越校验（不依赖库自身是否有此防护）。
   - `Newtonsoft.Json 11.0.2`（2018年），本身问题不大，但如果升级 .NET 版本，可以考虑升级到最新的 13.x 或迁移到 `System.Text.Json`。
   - 目标框架已升级为 `.NET Framework 4.8`（见批次0），不再是最初的 4.0，但仍是 .NET Framework 而非 .NET (Core)，如果要长期维护，可以评估未来迁移到 `.NET 8`（WPF 在 .NET 6+ 上也有官方支持，但要重写项目文件为 SDK 风格）。

5. ✅ **已修复（见"六.五、修复记录"第4条）** ~~Win32 结构体存在 64 位下的潜在隐患~~（`Win32.cs`）：
   - `JOBOBJECT_BASIC_LIMIT_INFORMATION`/`JOBOBJECT_EXTENDED_LIMIT_INFORMATION` 里对应 `SIZE_T`/`ULONG_PTR` 的字段已从 `uint` 改为 `System.UIntPtr`，正确匹配 64 位下的原生内存布局。

6. **UI 与业务逻辑高度耦合，无法做单元测试**：
   - `MainWindow.xaml.cs` 直接 `new NodeTree()`、直接读写文件系统路径、直接 `new ModProcessManager(...)`，没有任何接口抽象或依赖注入，导致业务逻辑无法脱离 WPF 窗口独立测试。
   - 如果之后要写单元测试或者做逻辑复用（比如未来做命令行版本/自动化脚本），建议把"扫描 Mods/构建树"、"运行游戏三步走"、"文件系统硬链接工具"这些抽出接口，UI 层只做调用与展示。

7. **无并发/多开保护**：
   - `RunningDirectory` 固定为 `Game/`（注释里提到"本可以用随机 GUID 目录名，但因为防火墙规则要求固定路径所以放弃了"），如果用户同时开两个 Mod 管理器实例并都点"运行"，两者会互相冲突（同时清空/写入同一个 `Game/` 目录）。目前没有加互斥锁（如文件锁/命名 Mutex）防止重复启动。

8. **`RunLegacyAsync`（Win7 及更早）体验很原始**：靠 `Thread.Sleep(10000)` 硬等 10 秒、再靠遍历全部系统进程判断 `MainModule.FileName` 路径是否含运行目录来"提示用户"，本质上不能保证准确性（多进程游戏、权限不足读不到其他进程的 `MainModule` 都会导致误判）。**（已分析，见"六.五、修复记录"批次2第1条：Win8+ 路径已是最佳实践无需改；Win7 分支若要清理需先确认是否还要兼容 Win7，未经确认不做代码改动）**
   - ✅ **Win8+ 路径的退出检测失效兜底已实现（见"六.五、修复记录"批次6-B）**：正常情况下 Win8+ 路径靠 Job Object 完成端口检测退出已经足够可靠，但用户反馈过极少数情况下检测会失效导致界面锁死、只能强杀 Mod 管理器丢存档。现已加入 `GameRunningWindow` + `ModProcessManager.TryForceTerminate()` 作为手动兜底通道，让用户在明确知道游戏已退出的情况下解锁而不跳过存档回写。这不能从根本上"修复"检测机制本身（依赖 Job Object 的可靠性，理论上仍存在极端情况下失效的可能），只是提供了不丢数据的退出路径。

9. ⚠️ **已尝试修复，但被证伪并回退（见"六.五、修复记录"批次2第2条 + 批次4）**：`app.manifest: requireAdministrator` 曾一度改为 `asInvoker`，但实测发现游戏本体（`Node.MainExecutable`）自身的可执行文件清单可能要求管理员权限，导致 Mod 管理器降权后再用 `CreateProcessW` 拉起游戏时直接失败（`Error 740 / ERROR_ELEVATION_REQUIRED`：Windows 不允许普通权限的父进程静默拉起要求提升权限的子进程）。现已改回 `requireAdministrator`。结论：至少对于要求管理员权限的游戏本体，Mod 管理器必须保持管理员权限运行，这不是 Mod 管理器自身的需求，而是被它拉起的目标程序的需求。

10. **没有测试项目、没有 CI**：仓库中只有一个 WPF 项目，没有任何自动化测试或构建校验，代码质量依赖人工。

11. ✅ **已实现（见"六.五、修复记录"批次2第3条）** ~~本地化/文案：UI 文案全英文，没有做多语言资源文件~~：已实现中英文双语界面 + 手动语言切换入口 + 记住用户选择，本地化字符串按用户要求保留为内嵌资源（非外部配置文件）。`AboutButton` 的作者邮箱/链接保持不译（属于产品/联系方式标识，非普通界面文案）。

## 八、后续编码前建议关注的点

- ~~在动手改动 `NodeTree`/`Node` 相关逻辑前，先决定要不要顺手修掉第 7 节第 1 条的"Files 目录校验"Bug~~ **（已完成，见"六.五、修复记录"）**
- 涉及"安装 Mod / 解压 zip"的功能改动时，注意 `SharpZipLib` 版本老旧的问题，如果要新增"从网络下载并自动安装 Mod"这类功能，务必先做路径穿越校验或升级压缩库。
- 涉及"运行游戏"流程的改动（`ModProcessManager`），要特别小心 `PrepareNode`/`CleanNode` 的递归硬链接+差异回写逻辑，这是全项目最核心也最脆弱的部分，改动前最好能有一个真实的 Mods/Profiles 测试环境验证硬链接行为（虚拟机或非系统盘的 NTFS 分区）。
- 如果要提升代码可维护性，可以考虑：把路径常量集中管理、把业务逻辑从 `MainWindow.xaml.cs` 中拆出（Service 层），为核心的 `NodeTree`/`IO` 逻辑补充单元测试（当前完全没有测试，补测试前需要先搭建测试项目和 Mock 文件系统的方式，因为很多逻辑直接依赖真实文件系统 API）。
- 如果考虑升级 .NET 版本或迁移到 SDK 风格项目，需要评估 WPF + P/Invoke（`Win32.cs`）+ Windows 特定 API 在新版本下的兼容性，这些 Win32 互操作代码本身与 .NET 版本无关，迁移风险主要在项目文件格式和 NuGet 包还原方式上。

## 九、关键文件速查表

| 文件 | 作用 |
|---|---|
| `MainWindow.xaml(.cs)` | 主界面，所有用户操作的入口（安装/删除/改名/隐藏/改ID/打开目录 Mod，新建/重命名/删除 Profile，运行游戏，切换语言，刷新） |
| `Node.cs` | 单个 Mod 节点的数据模型 + `node.json` 解析/写入（`Write` 方法） |
| `NodeTree.cs` | 扫描 `Mods/` 构建节点树、增删节点、只读校验（`ValidateNodes`）、ID 冲突检查与重新键入（`ContainsID`/`ReKeyNode`） |
| `ModItem.cs` | `Node` 的 UI 绑定包装（TreeView 用），含 `IsSelected`/`IsHidden` 状态和支持过滤的构造函数重载 |
| `ProfileItem.cs` | Profile 的 UI 绑定包装（ListView 用，含目录大小计算） |
| `ModProcessManager.cs` | 运行游戏的核心三步流程：准备硬链接 → 启动并等待退出 → 清理并回写存档 |
| `ModProcessManagerArguments.cs` | 传给 `ModProcessManager` 的参数 DTO |
| `IO.cs` | 硬链接/复制目录、判断两文件是否为同一物理文件、清空/删空目录、文件名净化（`PurifyFileName`）等工具方法 |
| `Win32.cs` | 全部 P/Invoke 声明（硬链接、Job Object、进程创建、IO 完成端口等） |
| `InputWindow.xaml(.cs)` | 通用的单行文本输入弹窗，支持预填默认文本 |
| `InstallModWizard.xaml(.cs)` | 手动安装向导：为没有预置 `node.json` 的 Mod 压缩包提供图形化配置界面（新增） |
| `FolderTreeNode.cs` | 安装向导用的轻量文件夹树节点，供选择"哪个文件夹的内容作为 Mod 文件"（新增） |
| `ModDisplayState.cs` | 持久化"隐藏"这个本地 UI 偏好到 `Mods/ModManagerState.json`（新增） |
| `ModIdChanger.cs` | Mod 改 ID 的级联迁移逻辑：子节点 `parent` 更新 + 所有 Profile 存档文件夹改名，含失败回滚（新增） |
| `SelectParentWindow.xaml(.cs)` | 右键改父 Mod 用的下拉框选择弹窗，候选项预先排除自身及子孙节点以避免造成环（新增，批次6-A） |
| `GameRunningWindow.xaml(.cs)` | 游戏运行期间显示的置顶非模态状态窗口，正常情况下自动关闭，标题栏关闭按钮被禁用，仅提供"强制解锁"按钮作为退出检测失效时的手动兜底（新增，批次6-B，仅 Win8+ 路径使用） |
| `LocalizationManager.cs` | 语言切换的应用/持久化/显示名称管理 |
| `Properties\Resources.resx` / `.Designer.cs` | 英文（默认）本地化字符串 + 手写的双语言选择逻辑 `GetLocalizedString` |
| `Properties\ResourcesChs.resx` | 简体中文本地化字符串（内嵌资源，非卫星程序集） |
| `Properties\Settings.settings` / `.Designer.cs` | 用户设置定义：`Language`（界面语言）、`LastProfileName`/`LastModID`（记住上次选择） |
| `App.config` | `ApplicationSettingsBase` 持久化用户设置所需的配置节；注意 `ClientSettingsSection`/`UserSettingsGroup` 所属程序集是 `System`，不是 `System.Configuration`（曾经写错过，见批次A的排查记录） |
| `app.manifest` | 声明运行权限级别（`requireAdministrator`，游戏本体可能要求提升权限，故 Mod 管理器必须保持一致，见批次4）、支持的 OS 版本 |
| `packages.config` / `Mo3ModManager.csproj` | 依赖包版本、项目/构建配置 |

### 批次6（右键补全其余 node.json 字段的修改入口 + 游戏运行中"强制解锁"兜底机制，均已手动验证通过）

这一批同样是根据用户实际使用反馈新增的功能性改动。按子功能记录：

**6-A：右键菜单补全剩余 node.json 字段的修改入口**

- **背景**：此前只有右键改名（`RenameModMenuItem_Click`）和右键改 ID（`ChangeModIdMenuItem_Click`，含级联迁移，见批次5-C）。`node.json` 里其余字段（`parent`/`main_executable`/`arguments`/`compatibility`）仍然只能手动改文件。这批把剩下的字段都补上了对应的右键菜单项。
- **改动内容**：
  - **改父 Mod**（`ChangeModParentMenuItem_Click`）：新增 `SelectParentWindow.xaml(.cs)`，用下拉框而非自由文本输入选择新的父 Mod（与其余字段用 `InputWindow` 自由文本输入不同）。原因：`parent` 字段是一个 ID 引用，自由文本输入容易打错字造成"引用不存在的父节点"的悬空引用，或者选择自己的子孙节点造成环。做法是调用 `NodeTree.GetNodeAndDescendants(node)` 算出该节点自身+全部子孙节点作为排除集合，下拉框候选项只呈现"排除集合之外"的节点，从**候选集合构造上**就不可能选出非法值，不需要运行时再做环检测。选择结果应用后：先把节点从旧父节点的 `Childs`（或 `RootNodes`，如果原来是根节点）里移除，更新 `ParentID`/`Parent`，再加入新父节点的 `Childs`（或 `RootNodes`），最后 `node.Write(...)` 落盘。因为树结构真的变了（不只是某个字段的值），所以复用 `BuildTreeView()` 整体重建 + `SelectModByID` 恢复选中，而不是像改名一样做局部 UI 更新。
  - **改主程序 / 改启动参数 / 改兼容模式**（`ChangeModMainExecutableMenuItem_Click` / `ChangeModArgumentsMenuItem_Click` / `ChangeModCompatibilityMenuItem_Click`）：三个都是简单的 `InputWindow` 自由文本输入 + 写回 `node.json`，与改名逻辑同构。**特别说明**：`MainExecutable` 字段允许被清空为空字符串（代表"这个节点是纯数据补丁，没有自己的可执行文件"，参见 `Node.IsRunnable`），所以这里没有像改名/改ID那样把 `String.IsNullOrWhiteSpace` 当作"用户取消"处理——因为 `InputWindow.ShowDialog` 对"用户取消"和"清空输入框后确认"这两种情况都返回 `String.Empty`，二者在这里本质上无法区分，但由于清空这个字段本身就是合法操作，这个歧义被认为是可以接受的（清空后 `Node.IsRunnable` 变为 false，UI 上会体现为该节点不可直接运行）。改完主程序后需要调用 `UpdateRunButtonStatus()` 刷新"运行"按钮的可用状态。
  - 所有右键菜单项统一沿用了批次5确立的预填充旧值的交互方式（`InputWindow.ShowDialog(..., 当前字段值)`），改父 Mod 的下拉框同理会默认选中当前父节点。
  - `ModItemContextMenu_Opened` 里"切换隐藏"菜单项的索引因为新插入了 4 个菜单项而发生偏移（从 `Items[5]` 变为 `Items[9]`），修改时同步检查更新，这是文档在"WPF 已知坑"里反复提醒过的按索引取菜单项的维护成本。
- **验证方式**：编译通过，手动对四个新菜单项各操作一次（改父 Mod 含"从根节点改成某子节点的子节点"和"从子节点改成根节点"两种方向，改主程序含"清空后该节点变为不可运行"），确认 `node.json` 内容和 UI 行为均符合预期，用户确认功能正常。

**6-B：游戏运行中"强制解锁"兜底机制**

- **背景问题**（用户提出）：偶尔游戏进程已经退出，但 Mod 管理器基于 Job Object 完成端口的正常退出检测机制（`ModProcessManager.RunStep2_RunAndWait`）未能感知到（比如某个子进程用某种方式脱离了 Job Object 的追踪范围），导致 Mod 管理器界面无限期锁死，用户唯一的出路是强制结束 Mod 管理器整个进程——但这样会跳过 `RunStep3_Clean`（负责把运行目录里变化的存档"收割"回 Profile 目录），导致下次启动前的清空动作会**连同尚未保存回 Profile 的存档一起丢弃**。
- **结论**：这是无法从根本上避免的（依赖 Windows Job Object 机制的可靠性，理论上存在极端情况下子进程逃逸出 Job 追踪范围的可能），但可以提供一个"用户明确确认游戏已退出"的手动兜底通道，避免用户唯一的选择是"强制杀 Mod 管理器丢存档"。
- **实现方式（用户在讨论中确认的方案：默认不弹窗打扰，只有正常检测机制失效时才需要用户手动介入）**：
  - 新增 `GameRunningWindow.xaml(.cs)`：一个小的、置顶、非模态的状态窗口，只在 Win8+ 路径下显示（Win7 及更早走的 `RunLegacyAsync` 本身不使用 Job Object、也不会无限期阻塞 UI 线程，不需要这个兜底）。窗口标题栏的关闭按钮被禁用（`Window_Closing` 里 `e.Cancel = true`，除非 `CloseProgrammatically()` 主动放行），因为这个窗口存在的意义就是"提醒用户游戏正在运行、别误操作"，不应该被用户随手关掉；唯一的退出路径是游戏正常退出被检测到（`RunWorkerCompleted` 回调里调用 `CloseProgrammatically()`）或用户点击"强制解锁"按钮并确认二次警告。
  - `ModProcessManager.cs` 新增 `TryForceTerminate()`：对当前 Job Object 里所有进程调用 `TerminateJobObject`，这会让 Job 的活跃进程数归零，从而让 `RunStep2_RunAndWait` 里原有的等待循环感知到 `JOB_OBJECT_MSG_ACTIVE_PROCESS_ZERO` 并正常返回——**关键点**：因为等待循环是"正常返回"而不是被外部杀死，后续的 `RunStep3_Clean`（存档回写）依然会照常执行。这正是"强制解锁"比"用户直接杀 Mod 管理器进程"更安全的原因：前者跳过的只是"等游戏自己退出"这一步，回写存档的清理流程完整保留；后者是连 Mod 管理器自己的清理逻辑也一起跳过。`jobHandle` 字段用 `jobHandleLock` 保护，因为它在后台线程（`RunStep2_RunAndWait`）写入、可能在 UI 线程（用户点击强制解锁按钮）读取。
  - `MainWindow.xaml.cs` 的 `RunButton_Click`：仅在 Win8+ 分支创建并显示 `GameRunningWindow`，订阅其 `ForceUnlockRequested` 事件调用 `modProcessManager.TryForceTerminate()`；`TryForceTerminate` 返回 `false`（没有活跃 Job 可终止，比如游戏恰好在用户点击按钮前一瞬间已经正常退出但 UI 线程还没来得及处理 `RunWorkerCompleted`）时不做任何事，因为 `RunWorkerCompleted` 很快会自己把窗口关掉。
  - 按钮文案和二次确认弹窗文案（`GameRunningWindow_ForceUnlockButton`/`GameRunningWindow_ConfirmForceUnlock`）明确提示"只有在游戏确实已经退出的情况下才使用此按钮，否则会丢失未保存的进度"，避免用户在游戏其实还在运行时误用导致真正丢失游戏内进度（这一步丢的是游戏本身的实时游玩进度，不是 Mod 管理器这边的存档同步逻辑问题）。
- **验证方式**：编译通过，手动运行游戏后确认 `GameRunningWindow` 正常显示且标题栏关闭按钮被禁用；点击"强制解锁"确认二次警告弹窗正常拦截误触；游戏正常退出时确认该窗口被自动关闭而不需要用户操作；未做"真实模拟 Job Object 检测失效"场景的自动化测试（这个场景本身就是一个依赖操作系统极端行为的边界情况，难以稳定复现，以用户手动运行验证 + 代码逻辑审查为主）。

### 批次7（安装 Mod 支持 zip/rar/7z 等多种压缩格式，已编译验证 + 自动化测试验证）

- **需求背景**：原来"安装 Mod"功能硬编码只认 zip（`ICSharpCode.SharpZipLib.Zip.FastZip`），但绝大多数 Mod 作者发布的压缩包并不限于 zip，常见的还有 rar、7z 等格式，用户体验受限。
- **改动内容**：
  - 移除 `SharpZipLib 0.86.0` 依赖（全代码库搜索确认无其他引用），改用 `SharpCompress` 库替代，支持 zip/rar/7z/tar/gz/bz2 等格式且自动按文件内容识别格式（不依赖扩展名）。
  - `IO.cs` 新增 `ExtractArchive(string ArchiveFilePath, string DestinationDirectory)` 方法：用 `SharpCompress.Archives.ArchiveFactory.Open(...)` 打开压缩包并遍历 `Entries`，对每个文件条目用 `WriteToFile(..., new ExtractionOptions { Overwrite = true, PreserveFileTime = true })` 解压，同时对每个条目的目标路径做 **zip slip（路径穿越）防护**：把目标路径 `Path.GetFullPath` 后与目标根目录做前缀比较，一旦条目名包含 `..\` 等导致逃出目标目录的路径，直接抛异常中止安装（这不是用户明确要求的，是换库时顺手补上的安全加固，原 SharpZipLib 老版本历史上就有过此类漏洞）。
  - `MainWindow.xaml.cs` 的 `InstallModButton_Click` 改为调用 `IO.ExtractArchive(...)`，替换原来的 `FastZip.ExtractZip(...)`。
  - 文件选择对话框的过滤器（`Resources.resx` / `ResourcesChs.resx` 的 `InstallMod_FileFilter`）从只列 `*.zip` 改为 `*.zip;*.rar;*.7z;*.tar;*.gz;*.bz2` + "所有文件 (*.*)"选项。
- **SharpCompress 选型的踩坑记录（值得记录，避免重复踩坑）**：
  - 一开始选用了当时 nuget.org 上的最新稳定版 **0.50.4**，但该版本对 .NET Framework 4.8 的 API 已经大幅重构：`ArchiveFactory.Open(string)` 方法被重命名/替换为 `OpenArchive(string)`；更麻烦的是，`IArchiveEntry` 上的 `WriteToFile` 扩展方法在这个版本**已被移除**（新版本只保留 `OpenEntryStream()` 这种流式 API，需要自己写 `entryStream.CopyTo(fileStream)` 才能落盘）。
  - 即使绕过 API 差异改用手写流拷贝让 0.50.4 编译通过，**运行时又炸了**：0.50.4 在 `net48` 目标下的 nuspec 依赖了 `Microsoft.Bcl.AsyncInterfaces 8.0.0` 和 `System.Text.Encoding.CodePages 8.0.0`，这两个包各自还有一串 polyfill 传递依赖（`System.Memory`/`System.Buffers`/`System.Runtime.CompilerServices.Unsafe`/`System.Threading.Tasks.Extensions`/`System.ValueTuple` 等）。这个项目是老式 `packages.config` 项目（非 SDK 风格、非 `PackageReference`），**不会自动解析传递依赖**，用最初的 `nuget.exe restore` 只装了 SharpCompress 自身，缺失的传递依赖会在运行到解压逻辑那一刻抛 `FileNotFoundException`（找不到 `System.Threading.Tasks.Extensions` 等程序集），且需要手动为每个缺失的包加 assembly binding redirect 才能彻底跑通，维护成本和复杂度远超预期。
  - **最终改用 SharpCompress 0.23.0**（nuget.org 历史版本，约2018年发布）。这个版本原生支持 `.NETFramework4.5`（`lib\net45\SharpCompress.dll`），**零额外依赖**，API 也是经典的 `ArchiveFactory.Open(string)` + `entry.WriteToFile(path, ExtractionOptions)` 形式，与本项目"老式 .NET Framework + packages.config"的技术栈完全匹配，不需要引入任何 polyfill 包或 binding redirect。经实测确认它仍然支持 zip/rar/7z/tar 等主流格式（本次只对 zip 和 tar 做了自动化验证，rar/7z 因本地无法快速构造测试样本、且 SharpCompress 对这两种格式的解压支持是稳定的历史功能，未单独测试，建议用户实际用真实 rar/7z 压缩包验证一次）。
  - **经验教训**：给"老式 .NET Framework + packages.config"项目选新依赖库时，不能只看"库是否支持某个 TargetFramework"，还要看该库在该 TargetFramework 下**是否需要额外的传递依赖**——这类项目没有 SDK 风格项目的自动传递依赖解析能力，选一个"专门为老框架做过精简 build、无额外依赖"的历史版本，往往比"用最新版本硬凹兼容性"更稳妥。
- **验证方式**：
  - `MSBuild.exe /t:Rebuild` 编译 0 错误 0 警告，`bin\Debug\Mo3ModManager.exe` 正常生成且体积/引用正常（`SharpCompress.dll` 约 520KB，无需额外拷贝任何 polyfill DLL）。
  - 启动 `Mo3ModManager.exe` 确认无崩溃（`tasklist` 确认 PID 存在、内存占用正常）。
  - 额外编写了独立的最小化验证程序（复用 `IO.ExtractArchive` 的同款解压逻辑代码，链接同一个 `SharpCompress.dll`，未纳入正式项目，验证后已删除）：
    - 用 `System.IO.Compression.ZipFile` 构造一个含嵌套子目录的测试 zip，解压后确认目录结构、文件内容与源文件完全一致。
    - 用 `SharpCompress.Writers.WriterFactory` 构造一个 tar 包（非 zip 格式，验证"自动识别格式而非认扩展名"这一核心诉求），解压后同样确认目录结构与内容正确，证明多格式支持确实生效而不仅限于 zip。
    - 构造一个包含 `..\..\evil.txt` 路径穿越条目的恶意 tar 包，确认 `ExtractArchive` 在写入任何文件之前就检测到并抛出异常中止，未在目标目录之外产生任何文件，确认 zip slip 防护生效。
  - 未做：真实 rar/7z 压缩包的端到端验证（本地环境无法快速无误地构造这两种专有格式的测试样本），建议用户用实际下载的 rar/7z 格式 Mod 包手动验证一次安装流程。


### 批次8（删除 Mod 后自动刷新 + 右键修改弹窗改为可调整大小，均已编译+手动验证通过）

**8-A：删除 Mod 后自动刷新**

- **背景问题**：`DeleteModButton_Click` 原来是手动做局部 UI 移除（不走 `BuildTreeView()` 整体重建），且这段代码里藏着一个复制粘贴 Bug：
  ```csharp
  if (selectedItem.Parent == null)
  {
      this.ProfilesListView.Items.Remove(selectedItem);   // 应该是 ModTreeView
  }
  else
  {
      selectedItem.Parent.Items.Remove(selectedItem);
  }
  ```
  删除**根节点** Mod 时，代码试图把它从 `ProfilesListView.Items`（Profile 列表）里移除，而不是 `ModTreeView`（Mod 树）——两者类型不同（`ProfileItem` vs `ModItem`），`Remove` 调用本身不会报错，只是什么都没删掉，于是被删除的根节点 Mod 会一直残留显示在 Mod 树里，直到用户手动点"刷新"按钮才会消失。这正是用户反馈的现象。
- **修复方式**：改为跟 `RefreshButton_Click` 一样调用 `this.BuildTreeView()` 重新扫描磁盘整体重建（用 `suppressSelectionPersistence` 包裹，避免重建过程中"暂时没有选中项"的瞬间状态被误当作用户主动清空选择而写入设置）。同时顺手补上：如果被删除的 Mod 正好是设置里记住的"上次选中的 Mod"（`LastModID`），一并清空该设置，避免下次启动时尝试恢复一个已经不存在的 Mod ID。
- **验证方式**：编译 0 错误 0 警告，`Mo3ModManager.exe` 正常启动无崩溃。逻辑审查确认：删除根节点 Mod 后不再需要手动刷新即可从树中消失；删除非根节点 Mod 的行为跟修复前一致（原来这条分支本身没有 Bug，只是也改用整体重建以保持两条分支一致、简化代码）。

**8-B：右键修改弹窗改为可调整大小**

- **背景问题**：右键改名 / 改主程序 / 改启动参数 / 改兼容模式共用的 `InputWindow`，以及右键改父 Mod 用的 `SelectParentWindow`，两者都把 `ResizeMode` 写成了 `NoResize`（固定 120x240 / 160x320）。当输入内容较长（比如启动参数或兼容模式字符串很长）时，文本框内容无法完整显示，用户也没有办法拖大窗口来看清楚。
- **修复方式**：
  - `InputWindow.xaml`：`ResizeMode` 改为 `CanResize`，加上 `MinHeight`/`MinWidth`（与原固定尺寸相同，防止拖得太小导致按钮和文本框挤在一起变形）。文本框所在行的 `RowDefinition` 从 `Height="Auto"` 改为 `Height="*"`（原来提示文字所在行才是 `*`，两者互换），并把 `TextBox` 的 `VerticalAlignment` 从 `Top` 改成 `Stretch` 加上 `VerticalScrollBarVisibility="Auto"`，这样窗口拉高之后文本框会跟着一起变高、多出来的空间能显示更多行文字，而不是窗口整体变高但文本框还是原来那么小。
  - `SelectParentWindow.xaml`：同样把 `ResizeMode` 改为 `CanResize` 并加上 `MinHeight`/`MinWidth`；这个窗口本身内容是一个下拉框（`ComboBox`），不像文本框那样需要额外空间展示多行内容，所以只改了 `ResizeMode`，没有改动内部布局。
  - 没有改动 `GameRunningWindow`（游戏运行中的状态窗口，见批次7-B）和 `InstallModWizard`（安装向导，本来就没设置 `ResizeMode`，默认已经是可调整大小），因为这次反馈只针对"右键修改 XX"系列的小弹窗。
- **验证方式**：编译 0 错误 0 警告，`Mo3ModManager.exe` 正常启动无崩溃。未做 UI 层面的像素级人工核对（比如拖动窗口边缘验证是否真的能拉伸、文本框是否真的跟着变大），因为这类纯样式改动风险很低，且 XAML 属性本身语义明确（`ResizeMode="CanResize"` + 行高 `*` 是 WPF 里让子控件跟随窗口拉伸的标准写法），建议用户实际拖一下这几个弹窗的边框确认视觉效果符合预期。

